<a href="<%= user_path(current_user) %>">
	<%= gravatar_for current_user, size: 52 %>
</a>
<h1>
	<%= current_user.name %>
</h1>
<span>
	<p><%= current_user.role %></p>
	<%= link_to "visit your profile", current_user %> to launch a new course
</span>