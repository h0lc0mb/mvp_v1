<% provide(:title, 'Browse courses') %>
<h1>Browse courses</h1>

<%= will_paginate %>

<ul class="courses">
	<% @courses.each do |course| %>
		<li>
			<%= link_to course.coursename, course %>
		</li>
	<% end %>
</ul>

<%= will_paginate %>