<% if signed_in? %>
	<div class="row">
		<aside class="span4">
			<section>
				<%= render 'shared/user_info' %>
			</section>
			<section>
				<%= render 'shared/stats' %>
			</section>
			<%-# I broke this :( But I don't want it on the home page anyway!-%>
			<%-#<section>-%>
				<%-#<%= render 'shared/course_form' -%>
			<%-#</section>-%>
		</aside>
		<div class="span8">
			<h3>Recent posts from your courses</h3>
			<%= render 'shared/feed' %>
		</div>
	</div>
<% else %>
	<div class="center hero-unit">
		<h1>Welcome to C4</h1>

		<h2>Change the way you learn.</h2>

		<%= link_to "Sign up", signup_path, class: "btn btn-large btn-primary" %>

		<p>
			
		<p>
			Already a member? 
			<%= link_to "Sign in", signin_path %>
		</p>
	</div>
<% end %>